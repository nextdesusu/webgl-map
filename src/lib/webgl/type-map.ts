import { Matrix3 } from "../math/matrix3";
import { Vector2 } from "../math/vector2";
import { GLTransferableType } from "./types";

type GLMethodName = string;
export const TYPE_MAP = new Map<GLTransferableType, GLMethodName>([
  // [Matrix3, ''],
  // [Vector2, ''],
  // []
])